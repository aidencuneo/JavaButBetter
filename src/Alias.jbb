ArrayList[Token] tokens
ArrayList[String] args

Alias(ArrayList[Token] tokens, ArrayList[String] args):
    this.tokens = tokens
    this.args = args

Alias(ArrayList[Token] tokens):
    this.tokens = tokens
    this.args = new ArrayList[String]()
