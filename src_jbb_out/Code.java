import java.io.*;
import java.util.*;

public class Code {
    @Override public void m1() {
        Extensions.operAdd(Extensions.operAdd(Extensions.operAdd(Extensions.operAdd("Extensions.operAdd(", lhs), ", "), rhs), ")");
        out += Extensions.operAdd(Extensions.operAdd(Extensions.operAdd(Extensions.operAdd("Extensions.operAdd(", lhs), ", "), rhs), ")");
        var a = Extensions.operMul("hi", 2);
        a = 10;
        var b = 2;
        b = 3;
        var d = Extensions.operAdd(true, true);
        var c = someClass.WHAT();
        return new SomeClass.Thing();
    }
    public static void main(String[] args) {
        var empty = List.of();
        empty = List.of(1, 2, 3);
        for (var i : LangUtil.asIterable(10)) {
            i = 20; {
                i = 30;
            }
            i = 40;
        }
        var i = 50;
        var lst = List.of(1, 2, 3, "hello");
        var ints = List.of(0, 1, 2, 3);
        LangUtil.println(LangUtil.slice(ints, 0, Extensions.operUnarySub(1), 1), LangUtil.slice(ints, null, null, Extensions.operUnarySub(1)), LangUtil.slice(ints, null, null, 1), LangUtil.slice(ints, null, 2, Extensions.operUnarySub(1)), LangUtil.slice(ints, 1, null, 1));
        LangUtil.println(Extensions.operEq("69", "69"));
    }
}

