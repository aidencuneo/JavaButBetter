import java.io.*;
import java.util.*;

public class Code {
    public static void main(String[] args) {
        var some1000_a = (Math.pow(1.0f, 1.0f) * 2 + 5 - 1);
        System.out.println("hello" + ", " + 10 + " days remaining");
        function ("hello");
        var n = 0.0f;
        if (LangUtil.isTruthy(some1000_a)) { n = 10.0f; }
        if (LangUtil.isTruthy(List.of (1 , 2 , "hi"))) { System.out.println("nice"); }
        if (LangUtil.isTruthy(List.of (1))) { System.out.println("yeah"); }
        var i = 3;
        var j = 3;
        var k = 3;
        while (LangUtil.isTruthy(k)) {
            System.out.println(i -- + ", " + j -- + ", " + k --);
        }
    }
    public int max(int a , int b) {
        return a > b ? a : b;
    }
    public static String what() {
        var i = 0;
        ++ i;
        i += 1;
        var s = "" + i;
        return null;
    }
    public static void function(String s) {
        String h = "what" + s;
        h = h.substring (1);
    }
    public static void dosomething() {
        System.out.println("Something done");
    }
}
class Point {
    int x = 0;
    int y = 0;
    public Point() {
        var newx = x;
        var newy = y;
    }
}

